import React, { Component } from "react";
import { hot } from "react-hot-loader";

import "./App.css";

const App = () => (
  <div className="App">
    <h1> Welcome to Ocular React! </h1>
  </div>
);

export default hot(module)(App);
